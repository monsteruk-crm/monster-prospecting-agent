# Status

- Current milestone: Act 1 — governed knowledge ingestion, checkpointed discovery, scored dossiers, human-reviewed first moves, operational settings/usage visibility and CRM dry-run validation.
- What works now: Scout home with persisted recent missions/prospects, a progressive New mission builder, dedicated Runs history and run dossier screens, secondary Diagnostics for gateway/health checks, full streamed stage/query output, durable executed-run history and run-ID retrieval, bounded same-run deeper search continuation, a full persisted mission-brief editor and dossier brief panel, explicit public-email-only contact requirements with deterministic filtering, visible verified public email/contact-page routes, bounded mission launch and named progress states, an approved-dossier CSV download action, central Zod model registry, AI Gateway smoke route, Prisma schema/configuration, applied Prisma Postgres migrations, LangGraph Postgres checkpoints, database and observability health routes, validated sales-mission brief with a five-account MVP target, LangGraph target-profile/search-strategy preparation graph, default DuckDuckGo search-provider adapter, bounded search-provider-to-safe-fetch discovery graph with pre-fetch non-first-party filtering, SSRF-safe official-source fetching with visible partial errors, typed account extraction, buying-signal verification with deterministic provenance/freshness checks, idempotent mission/run/account/evidence/signal/audit persistence, deterministic capped account scores, provenance-linked role/contact-page routes with mission buyer-role fallback, persisted review decisions, structured research-gap capture, checkpoint resume routes, approved first-move drafts with bounded Monster knowledge retrieval, a stable retriever abstraction with the local lexical adapter, exact-schema approved-record lead CSV dry-run export, CRM dry-run validation, `GET /api/runs/:id` dossier reads, and deterministic authority-tagged ingestion of the restored checklist and positioning sources.
- Intentionally not implemented: Brave or other paid search-provider adapters, embeddings, pgvector semantic retrieval, hybrid retrieval, broad public contact crawling, automatic outreach, live Monster CRM writes (explicit future TODO), SSO/token rotation, production organisation-level RBAC, and general user authentication. Provider dollar pricing remains unknown unless returned by the model provider; the usage ledger does not invent spend.
- Contact intelligence boundary: safe-fetch now retains capped official-page links, canonical URLs, explicit `mailto:`/`tel:` and JSON-LD contact hints; bounded post-canonicalisation enrichment checks linked pages, one focused same-site DuckDuckGo search and up to three official contact pages per account. `PUBLIC_EMAIL` eligibility is evaluated after that attempt, and a per-account contact-enrichment route is available without rerunning market discovery.
- Active blockers: some third-party official sites still return HTTP 403 to the safe fetcher; these remain visible partial errors and are never bypassed. DuckDuckGo hosted egress can return HTTP 403/202 challenge responses; the provider detects those responses and uses a bounded no-key Bing HTML fallback, while retaining explicit errors if both providers fail. LangSmith tracing configuration is present and was used during the live model evaluation, but delivery to the LangSmith workspace was not independently confirmed.
- Verification performed: restored `knowledge/authoritative/full-checklist.md` and `knowledge/positioning/product-decks-rag-addendum.md` from matching workspace source copies, `npm run knowledge:ingest` generated 41 chunks from 2 active governed sources with stable source hashes, the expanded `npm run knowledge:evaluate` lexical baseline achieved retrieval hit@3 0.70 and mean expected-section recall 0.60, and `npm run knowledge:evaluate -- --live` produced 3/3 schema-valid first-move drafts with proxy scores of 0.80, 1.00 and 0.80, the reported Trustpilot/Indeed/MyFeteTickets/F6S/TodayTix/BusinessWire candidates are now filtered before fetch, extracted accounts with no roles now receive mission buyer-role fallbacks, role-only routes no longer receive the hardcoded no-route score cap, bounded contact enrichment retains official-page metadata and evaluates email-only constraints after the contact pass, search progress callbacks report executed query/result counts and continuation seeds saved search/source/account/signal state, reconciled `docs/contracts/lead_sheet_schema.csv` exactly with the export header, CRM dry-run authentication checks returned 401/403/200 for unauthenticated, wrong-organisation and authorised requests, and the DuckDuckGo-compatible local smoke now follows the working browser-like GET shape and returns three results via the bounded Bing fallback when DuckDuckGo responds with HTTP 202. The UI refresh also passed `npm run lint`, `npm run typecheck`, `npm test` (59/59), `npm run build`, `npx playwright test tests/e2e/shell.spec.ts`, route smoke checks for `/`, `/missions/new`, `/runs`, `/settings`, `/costs` and `/diagnostics`, and `git diff --check`. A Prisma migration attempt was intentionally not applied because the database has existing LangGraph checkpoint-table drift; no reset was performed. No live CRM write occurred.
- Next three actions: manually run a mission through `/missions/new` and inspect the live `/runs/:id` dossier; exercise review/export and reopen from `/runs` on desktop and mobile; add token rotation/SSO when the deployment identity is available. Live CRM insertion remains deferred post-MVP TODO work.
- Last updated: 2026-08-05; commit hash unavailable. The operational-settings and usage migration is applied to Prisma Postgres.
