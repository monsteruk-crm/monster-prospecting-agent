import Link from "next/link";

export function RunsPage({ runs }: { runs: Array<{ id: string; status: string; discoveryStage: string; startedAt: Date; completedAt: Date | null; mission: { name: string; owner: string }; review: { status: string } | null }> }) {
  return <section><p className="text-xs font-bold uppercase tracking-[0.2em] text-[#f5c542]">History</p><h1 className="mt-2 text-4xl font-black">Executed runs</h1><p className="mt-3 text-white/55">Every run keeps its mission brief, settings snapshot, evidence, review state and usage trail.</p><div className="mt-8 overflow-x-auto rounded-2xl border border-white/10"><table className="w-full min-w-[760px] text-left text-sm"><thead className="bg-white/[0.04] text-xs uppercase tracking-[0.12em] text-white/45"><tr><th className="p-4">Mission</th><th className="p-4">Status</th><th className="p-4">Stage</th><th className="p-4">Started</th><th className="p-4">Review</th><th className="p-4" /></tr></thead><tbody>{runs.map((run) => <tr key={run.id} className="border-t border-white/10"><td className="p-4"><p className="font-semibold">{run.mission.name}</p><p className="text-xs text-white/40">{run.mission.owner}</p><p className="mt-1 font-mono text-[10px] text-white/30">{run.id}</p></td><td className="p-4 text-white/65">{run.status}</td><td className="p-4 text-white/65">{run.discoveryStage}</td><td className="p-4 text-white/50">{new Date(run.startedAt).toLocaleString()}</td><td className="p-4 text-white/65">{run.review?.status ?? "—"}</td><td className="p-4"><Link href={`/runs/${run.id}`} className="text-[#f5c542] hover:underline">Open</Link></td></tr>)}</tbody></table>{runs.length === 0 ? <p className="p-8 text-white/45">No executed runs yet.</p> : null}</div></section>;
}
