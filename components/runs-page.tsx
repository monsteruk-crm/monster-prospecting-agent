import Link from "next/link";

export function RunsPage({ runs }: { runs: Array<{ id: string; status: string; discoveryStage: string; startedAt: Date; completedAt: Date | null; mission: { name: string; owner: string }; review: { status: string } | null }> }) {
  return <section className="mx-auto max-w-6xl"><header className="page-heading"><div><p className="eyebrow">Mission history</p><h1>Your hunts.</h1><p>Reopen a mission to inspect evidence, review prospects, or continue research.</p></div><Link href="/missions/new" className="primary-button">New mission</Link></header><div className="mt-8 space-y-3">{runs.map((run) => <Link key={run.id} href={`/runs/${run.id}`} className="major-surface flex flex-col gap-4 transition hover:border-[#f5c542]/40 sm:flex-row sm:items-center sm:justify-between"><div><p className="text-xl font-bold">{run.mission.name}</p><p className="mt-1 text-sm text-white/45">{run.mission.owner} · {new Date(run.startedAt).toLocaleDateString()} · {run.discoveryStage}</p></div><div className="flex items-center gap-3"><span className="status">{run.review?.status ?? run.status}</span><span className="text-sm text-[#f5c542]">Open →</span></div></Link>)}{runs.length === 0 ? <div className="empty-state"><p>No missions yet.</p><p className="mt-1 text-sm text-white/50">Define a market and Scout will start building your first shortlist.</p></div> : null}</div></section>;
}
